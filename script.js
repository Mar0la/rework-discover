function toggleMode() {
  
  const html = document.documentElement;
  html.classList.toggle('light')

  // const img = document.querySelector("#profile img")
  // if(html.classList.contains('light')) {
  //   imh.setAttribute('src', img)
  // } else {
  //   img.setAttribute('src', img)
  // }
}